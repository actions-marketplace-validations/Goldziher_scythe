name: Integration Tests

# The path filter is the list of things that can change generated output. It
# was previously narrower than that list, and the gap was not academic:
#
#   - crates/scythe-backend/** *is* the renderer (renderer.rs, manifest.rs,
#     naming.rs, types.rs). A naming-convention change there rewrites every
#     backend's output for every language and triggered nothing at all.
#   - tools/integration-test-generator/** owns the *.jinja harness templates.
#     A formatter mangled them once already -- see the comment at ci.yml:167.
#   - This file was absent from its own filter, so a change that broke the
#     workflow could not run the workflow that would have reported it.
on:
  push:
    branches: [main]
    paths:
      - "crates/scythe-codegen/**"
      - "crates/scythe-core/**"
      - "crates/scythe-backend/**"
      - "crates/scythe-cli/**"
      - "integration_tests/**"
      - "tools/integration-test-generator/**"
      - "testing_data/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/integration.yml"
  pull_request:
    branches: [main]
    paths:
      - "crates/scythe-codegen/**"
      - "crates/scythe-core/**"
      - "crates/scythe-backend/**"
      - "crates/scythe-cli/**"
      - "integration_tests/**"
      - "tools/integration-test-generator/**"
      - "testing_data/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/integration.yml"
  workflow_dispatch:

concurrency:
  group: integration-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  integration-pg:
    name: PostgreSQL Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: scythe
          POSTGRES_PASSWORD: scythe
          POSTGRES_DB: scythe_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgres://scythe:scythe@localhost:5432/scythe_test

    steps:
      - uses: actions/checkout@v7.0.1

      - name: Run PG schema migration
        run: PGPASSWORD=scythe psql -h localhost -U scythe -d scythe_test -f integration_tests/sql/pg/schema.sql

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            integration_tests/rust-sqlx -> target
            integration_tests/rust-sqlx-nested-json -> target
            integration_tests/rust-tokio-postgres -> target
      # ~keep go-version-file (not a literal pin) for every generated Go project
      # in this workflow: those go.mod files are rendered from
      # tools/integration-test-generator/templates/go.mod.jinja and can
      # require a newer Go than any literal pinned here. This used to
      # succeed anyway because actions/setup-go left GOTOOLCHAIN=auto, so a
      # stale literal pin silently downloaded the toolchain go.mod actually
      # required. Newer setup-go versions default GOTOOLCHAIN=local, turning
      # that mismatch into a hard failure ("go.mod requires go >= X (running
      # go Y; GOTOOLCHAIN=local)"). Pointing at the job's own generated
      # go.mod keeps the two from drifting again — do not re-pin a literal.
      - uses: actions/setup-go@v7
        with:
          go-version-file: integration_tests/go-pgx/go.mod
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"
      # csproj.jinja pins <TargetFramework>net10.0</TargetFramework>. Without this
      # check a dotnet-version drift back toward 8.0 above could still build
      # silently by falling back to a pre-existing SDK 10 on the runner image,
      # hiding the drift instead of failing it.
      - name: Verify the installed .NET SDK matches the declared TargetFramework
        run: |
          set -euo pipefail
          if ! dotnet --list-sdks | grep -qE '^10\.'; then
            echo "::error::No .NET 10 SDK resolved; csproj.jinja's net10.0 TargetFramework requires one."
            exit 1
          fi
      - uses: erlef/setup-beam@v1
        with:
          otp-version: "26"
          elixir-version: "1.16"
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          # The pinned Ruby ships a Bundler older than 4.x, but the committed Gemfile.lock files
          # are written by Bundler 4. An older Bundler cannot read a newer
          # lockfile: it re-execs to install the newer one mid-step and then
          # fails to resolve gems. A newer Bundler reads older lockfiles fine.
          bundler: latest
      - uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: pdo_pgsql

      - run: pip install uv
      # Same pnpm major as ci.yml's generated-query-code gate, so a new pnpm
      # release cannot change the answer under one and not the other. No
      # `runtime` input: `actions/setup-node` above already provides node 22.
      # `install: false` because there is no root package.json -- each project
      # below runs its own `pnpm install`.
      - uses: pnpm/setup@v2
        with:
          version: "11"
          install: false
          cache: true
          cache-dependency-path: integration_tests/*/pnpm-lock.yaml
      # allowBuilds (pnpm-workspace.yaml.jinja:1) first appeared in pnpm 10.26 and
      # became the sole build-permission mechanism in pnpm 11 (onlyBuiltDependencies
      # etc. removed). Below that floor an unknown `allowBuilds` key is silently
      # ignored -- exit 0, no warning -- and better-sqlite3/oracledb's postinstall
      # builds are skipped.
      - name: Verify pnpm resolved a version that honours allowBuilds
        run: |
          set -euo pipefail
          resolved="$(pnpm --version)"
          major="${resolved%%.*}"
          rest="${resolved#*.}"
          minor="${rest%%.*}"
          if [ "$major" -lt 10 ] || { [ "$major" -eq 10 ] && [ "$minor" -lt 26 ]; }; then
            echo "::error::pnpm $resolved predates allowBuilds (needs >=10.26); pnpm-workspace.yaml's allowBuilds key would be silently ignored."
            exit 1
          fi

      - name: Test rust-sqlx
        working-directory: integration_tests/rust-sqlx
        run: cargo run --quiet

      - name: Test rust-sqlx-nested-json
        working-directory: integration_tests/rust-sqlx-nested-json
        run: cargo run --quiet

      - name: Test rust-tokio-postgres
        working-directory: integration_tests/rust-tokio-postgres
        run: cargo run --quiet

      - name: Test python-psycopg3
        working-directory: integration_tests/python-psycopg3
        run: uv run python test_integration.py

      - name: Test python-asyncpg
        working-directory: integration_tests/python-asyncpg
        run: uv run python test_integration.py

      - name: Test python-psycopg3-pydantic
        working-directory: integration_tests/python-psycopg3-pydantic
        run: uv run python test_integration.py

      - name: Test python-psycopg3-msgspec
        working-directory: integration_tests/python-psycopg3-msgspec
        run: uv run python test_integration.py

      - name: Test go-pgx
        working-directory: integration_tests/go-pgx
        run: go mod tidy && go run .

      - name: Test typescript-postgres
        working-directory: integration_tests/typescript-postgres
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-pg
        working-directory: integration_tests/typescript-pg
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-pg-camel
        working-directory: integration_tests/typescript-pg-camel
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-pg-zod
        working-directory: integration_tests/typescript-pg-zod
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-pg-outer-join-unions
        working-directory: integration_tests/typescript-pg-outer-join-unions
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-pg-zod-outer-join-unions
        working-directory: integration_tests/typescript-pg-zod-outer-join-unions
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      # structs_only has no runtime surface, so the gate is that the emitted
      # file type-checks and contains neither the driver import nor a query
      # function.
      - name: Test typescript-pg-structs-only
        working-directory: integration_tests/typescript-pg-structs-only
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts
          # Both assertions below are negative -- "the file must NOT contain
          # X" -- and `grep -q` on a file that does not exist exits 2, which
          # takes the same `if` branch as "pattern not found". So a renamed or
          # missing generated/queries.ts made this step report success while
          # asserting nothing about anything. Establish the subject first:
          # the file must exist and must actually be the structs_only output
          # (row interfaces and nothing else), so the two greps below are
          # searching something real.
          if [ ! -f generated/queries.ts ]; then
            echo "FAIL: generated/queries.ts is missing, so the structs_only assertions below would pass vacuously" >&2
            exit 1
          fi
          structs=$(grep -cE '^export (interface|type) ' generated/queries.ts || true)
          echo "structs_only output declares $structs exported interface/type(s)."
          if [ "$structs" -eq 0 ]; then
            echo "FAIL: structs_only output declares no exported interface or type. It is supposed to be nothing but row structs, so an empty one means the generator emitted nothing and the two 'must not contain' assertions below are checking an empty file." >&2
            exit 1
          fi
          if grep -q 'from "pg"' generated/queries.ts; then
            echo "FAIL: structs_only output must not import the pg driver" >&2
            exit 1
          fi
          if grep -qE '^export async function ' generated/queries.ts; then
            echo "FAIL: structs_only output must not emit query functions" >&2
            exit 1
          fi

      - name: Test typescript-kysely
        working-directory: integration_tests/typescript-kysely
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-kysely-camel
        working-directory: integration_tests/typescript-kysely-camel
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test java-jdbc
        working-directory: integration_tests/java-jdbc
        run: mvn -q compile exec:java

      - name: Test kotlin-jdbc
        working-directory: integration_tests/kotlin-jdbc
        run: gradle run --quiet

      - name: Test java-r2dbc
        working-directory: integration_tests/java-r2dbc
        run: mvn -q compile exec:java

      - name: Test kotlin-r2dbc
        working-directory: integration_tests/kotlin-r2dbc
        run: gradle run --quiet

      - name: Test kotlin-exposed
        working-directory: integration_tests/kotlin-exposed
        run: gradle run --quiet

      - name: Test kotlin-jdbc-ext
        working-directory: integration_tests/kotlin-jdbc-ext
        run: gradle run --quiet

      - name: Test csharp-npgsql
        working-directory: integration_tests/csharp-npgsql
        run: dotnet run

      - name: Test elixir-postgrex
        working-directory: integration_tests/elixir-postgrex
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get
          mix run test/integration_test.exs

      - name: Test elixir-ecto
        working-directory: integration_tests/elixir-ecto
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get
          mix run test/integration_test.exs

      - name: Test ruby-pg
        working-directory: integration_tests/ruby-pg
        run: |
          bundle install
          bundle exec ruby test_integration.rb

      - name: Test php-pdo
        working-directory: integration_tests/php-pdo
        run: php test_integration.php

      # php-pdo-namespace has no runtime harness -- it exists to prove the
      # generator can target a custom PHP namespace, so the test is a lint of
      # the emitted file rather than a DB-backed run. `php -l` only checks
      # syntax; it cannot catch a wrong namespace *value* or any query-level
      # regression, which is why the grep below asserts the actual namespace
      # string, not just that the file parses.
      - name: Test php-pdo-namespace
        working-directory: integration_tests/php-pdo-namespace
        run: |
          php -l generated/queries.php
          grep -qF 'namespace App\Database\Generated;' generated/queries.php

      - name: Test php-amphp
        working-directory: integration_tests/php-amphp
        run: |
          composer install --quiet
          php test_integration.php

  integration-mysql:
    name: MySQL Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: scythe
          MYSQL_DATABASE: scythe_test
          MYSQL_USER: scythe
          MYSQL_PASSWORD: scythe
        ports:
          - 3306:3306
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: mysql://scythe:scythe@127.0.0.1:3306/scythe_test
      MYSQL_URL: mysql://scythe:scythe@127.0.0.1:3306/scythe_test

    steps:
      - uses: actions/checkout@v7.0.1

      - name: Run MySQL schema migration
        run: mysql -h 127.0.0.1 -u scythe -pscythe scythe_test < integration_tests/sql/mysql/schema.sql

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            integration_tests/rust-sqlx-mysql -> target
      - uses: actions/setup-go@v7
        with:
          go-version-file: integration_tests/go-database-sql-mysql/go.mod
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"
      # csproj.jinja pins <TargetFramework>net10.0</TargetFramework>. Without this
      # check a dotnet-version drift back toward 8.0 above could still build
      # silently by falling back to a pre-existing SDK 10 on the runner image,
      # hiding the drift instead of failing it.
      - name: Verify the installed .NET SDK matches the declared TargetFramework
        run: |
          set -euo pipefail
          if ! dotnet --list-sdks | grep -qE '^10\.'; then
            echo "::error::No .NET 10 SDK resolved; csproj.jinja's net10.0 TargetFramework requires one."
            exit 1
          fi
      - uses: erlef/setup-beam@v1
        with:
          otp-version: "26"
          elixir-version: "1.16"
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          # The pinned Ruby ships a Bundler older than 4.x, but the committed Gemfile.lock files
          # are written by Bundler 4. An older Bundler cannot read a newer
          # lockfile: it re-execs to install the newer one mid-step and then
          # fails to resolve gems. A newer Bundler reads older lockfiles fine.
          bundler: latest
      - uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: pdo_mysql

      - run: pip install uv
      # Same pnpm major as ci.yml's generated-query-code gate, so a new pnpm
      # release cannot change the answer under one and not the other. No
      # `runtime` input: `actions/setup-node` above already provides node 22.
      # `install: false` because there is no root package.json -- each project
      # below runs its own `pnpm install`.
      - uses: pnpm/setup@v2
        with:
          version: "11"
          install: false
          cache: true
          cache-dependency-path: integration_tests/*/pnpm-lock.yaml
      # allowBuilds (pnpm-workspace.yaml.jinja:1) first appeared in pnpm 10.26 and
      # became the sole build-permission mechanism in pnpm 11 (onlyBuiltDependencies
      # etc. removed). Below that floor an unknown `allowBuilds` key is silently
      # ignored -- exit 0, no warning -- and better-sqlite3/oracledb's postinstall
      # builds are skipped.
      - name: Verify pnpm resolved a version that honours allowBuilds
        run: |
          set -euo pipefail
          resolved="$(pnpm --version)"
          major="${resolved%%.*}"
          rest="${resolved#*.}"
          minor="${rest%%.*}"
          if [ "$major" -lt 10 ] || { [ "$major" -eq 10 ] && [ "$minor" -lt 26 ]; }; then
            echo "::error::pnpm $resolved predates allowBuilds (needs >=10.26); pnpm-workspace.yaml's allowBuilds key would be silently ignored."
            exit 1
          fi

      - name: Test rust-sqlx-mysql
        working-directory: integration_tests/rust-sqlx-mysql
        run: cargo run --quiet

      - name: Test go-database-sql-mysql
        working-directory: integration_tests/go-database-sql-mysql
        run: go mod tidy && go run .

      - name: Test python-aiomysql
        working-directory: integration_tests/python-aiomysql
        run: uv run python test_integration.py

      - name: Test typescript-mysql2
        working-directory: integration_tests/typescript-mysql2
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-kysely-mysql2
        working-directory: integration_tests/typescript-kysely-mysql2
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test java-jdbc-mysql
        working-directory: integration_tests/java-jdbc-mysql
        run: mvn -q compile exec:java

      - name: Test kotlin-jdbc-mysql
        working-directory: integration_tests/kotlin-jdbc-mysql
        run: gradle run --quiet

      - name: Test csharp-mysqlconnector
        working-directory: integration_tests/csharp-mysqlconnector
        run: dotnet run

      - name: Test elixir-myxql
        working-directory: integration_tests/elixir-myxql
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get
          mix run test/integration_test.exs

      - name: Test ruby-mysql2
        working-directory: integration_tests/ruby-mysql2
        run: |
          bundle install
          bundle exec ruby test_integration.rb

      - name: Test ruby-trilogy
        working-directory: integration_tests/ruby-trilogy
        run: |
          bundle install
          bundle exec ruby test_integration.rb

      - name: Test php-pdo-mysql
        working-directory: integration_tests/php-pdo-mysql
        run: php test_integration.php

      - name: Test php-amphp-mysql
        working-directory: integration_tests/php-amphp-mysql
        run: |
          composer install --quiet
          php test_integration.php

  integration-mariadb:
    name: MariaDB Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      mariadb:
        image: mariadb:11
        env:
          MARIADB_ROOT_PASSWORD: scythe
          MARIADB_DATABASE: scythe_test
          MARIADB_USER: scythe
          MARIADB_PASSWORD: scythe
        ports:
          - 3306:3306
        options: >-
          --health-cmd "healthcheck.sh --connect --innodb_initialized"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    env:
      MARIADB_URL: mysql://scythe:scythe@127.0.0.1:3306/scythe_test

    steps:
      - uses: actions/checkout@v7.0.1

      - name: Run MariaDB schema migration
        run: mysql -h 127.0.0.1 -u scythe -pscythe scythe_test < integration_tests/sql/mariadb/schema.sql

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            integration_tests/rust-sqlx-mariadb -> target
      - uses: actions/setup-go@v7
        with:
          go-version-file: integration_tests/go-database-sql-mariadb/go.mod
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"
      # csproj.jinja pins <TargetFramework>net10.0</TargetFramework>. Without this
      # check a dotnet-version drift back toward 8.0 above could still build
      # silently by falling back to a pre-existing SDK 10 on the runner image,
      # hiding the drift instead of failing it.
      - name: Verify the installed .NET SDK matches the declared TargetFramework
        run: |
          set -euo pipefail
          if ! dotnet --list-sdks | grep -qE '^10\.'; then
            echo "::error::No .NET 10 SDK resolved; csproj.jinja's net10.0 TargetFramework requires one."
            exit 1
          fi
      - uses: erlef/setup-beam@v1
        with:
          otp-version: "26"
          elixir-version: "1.16"
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          # The pinned Ruby ships a Bundler older than 4.x, but the committed Gemfile.lock files
          # are written by Bundler 4. An older Bundler cannot read a newer
          # lockfile: it re-execs to install the newer one mid-step and then
          # fails to resolve gems. A newer Bundler reads older lockfiles fine.
          bundler: latest
      - uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: pdo_mysql

      - run: pip install uv
      # Same pnpm major as ci.yml's generated-query-code gate, so a new pnpm
      # release cannot change the answer under one and not the other. No
      # `runtime` input: `actions/setup-node` above already provides node 22.
      # `install: false` because there is no root package.json -- each project
      # below runs its own `pnpm install`.
      - uses: pnpm/setup@v2
        with:
          version: "11"
          install: false
          cache: true
          cache-dependency-path: integration_tests/*/pnpm-lock.yaml
      # allowBuilds (pnpm-workspace.yaml.jinja:1) first appeared in pnpm 10.26 and
      # became the sole build-permission mechanism in pnpm 11 (onlyBuiltDependencies
      # etc. removed). Below that floor an unknown `allowBuilds` key is silently
      # ignored -- exit 0, no warning -- and better-sqlite3/oracledb's postinstall
      # builds are skipped.
      - name: Verify pnpm resolved a version that honours allowBuilds
        run: |
          set -euo pipefail
          resolved="$(pnpm --version)"
          major="${resolved%%.*}"
          rest="${resolved#*.}"
          minor="${rest%%.*}"
          if [ "$major" -lt 10 ] || { [ "$major" -eq 10 ] && [ "$minor" -lt 26 ]; }; then
            echo "::error::pnpm $resolved predates allowBuilds (needs >=10.26); pnpm-workspace.yaml's allowBuilds key would be silently ignored."
            exit 1
          fi

      - name: Test rust-sqlx-mariadb
        working-directory: integration_tests/rust-sqlx-mariadb
        env:
          DATABASE_URL: ${{ env.MARIADB_URL }}
        run: cargo run --quiet

      - name: Test go-database-sql-mariadb
        working-directory: integration_tests/go-database-sql-mariadb
        run: go mod tidy && go run .

      - name: Test python-aiomysql-mariadb
        working-directory: integration_tests/python-aiomysql-mariadb
        run: uv run python test_integration.py

      - name: Test typescript-mysql2-mariadb
        working-directory: integration_tests/typescript-mysql2-mariadb
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-kysely-mariadb
        working-directory: integration_tests/typescript-kysely-mariadb
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test java-jdbc-mariadb
        working-directory: integration_tests/java-jdbc-mariadb
        run: mvn -q compile exec:java

      - name: Test kotlin-jdbc-mariadb
        working-directory: integration_tests/kotlin-jdbc-mariadb
        run: gradle run --quiet

      - name: Test csharp-mysqlconnector-mariadb
        working-directory: integration_tests/csharp-mysqlconnector-mariadb
        run: dotnet run

      - name: Test elixir-myxql-mariadb
        working-directory: integration_tests/elixir-myxql-mariadb
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get
          mix run test/integration_test.exs

      - name: Test ruby-mysql2-mariadb
        working-directory: integration_tests/ruby-mysql2-mariadb
        run: |
          bundle install
          bundle exec ruby test_integration.rb

      - name: Test ruby-trilogy-mariadb
        working-directory: integration_tests/ruby-trilogy-mariadb
        run: |
          bundle install
          bundle exec ruby test_integration.rb

      - name: Test php-pdo-mariadb
        working-directory: integration_tests/php-pdo-mariadb
        run: php test_integration.php

  integration-oracle:
    name: Oracle Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      oracle:
        image: gvenzl/oracle-xe:21-slim
        env:
          ORACLE_PASSWORD: scythe
          APP_USER: scythe
          APP_USER_PASSWORD: scythe
        ports:
          - 1521:1521
        options: >-
          --health-cmd "healthcheck.sh"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 20

    env:
      ORACLE_URL: oracle://scythe:scythe@localhost:1521/XEPDB1

    steps:
      - uses: actions/checkout@v7.0.1

      - name: Run Oracle schema migration
        run: |
          sudo apt-get update && sudo apt-get install -y libaio1t64 alien
          sudo ln -sf /usr/lib/x86_64-linux-gnu/libaio.so.1t64 /usr/lib/x86_64-linux-gnu/libaio.so.1 || true
          wget -q https://download.oracle.com/otn_software/linux/instantclient/2340000/oracle-instantclient-basic-23.4.0.24.05-1.el9.x86_64.rpm
          wget -q https://download.oracle.com/otn_software/linux/instantclient/2340000/oracle-instantclient-sqlplus-23.4.0.24.05-1.el9.x86_64.rpm
          wget -q https://download.oracle.com/otn_software/linux/instantclient/2340000/oracle-instantclient-devel-23.4.0.24.05-1.el9.x86_64.rpm
          sudo alien -i oracle-instantclient-basic-*.rpm
          sudo alien -i oracle-instantclient-sqlplus-*.rpm
          sudo alien -i oracle-instantclient-devel-*.rpm
          sudo ln -sf libclntsh.so.23.1 /usr/lib/oracle/23/client64/lib/libclntsh.so
          echo "/usr/lib/oracle/23/client64/lib" | sudo tee /etc/ld.so.conf.d/oracle.conf
          sudo ldconfig
          export LD_LIBRARY_PATH="/usr/lib/oracle/23/client64/lib:${LD_LIBRARY_PATH:-}"
          export PATH=/usr/lib/oracle/23/client64/bin:$PATH
          sqlplus scythe/scythe@localhost:1521/XEPDB1 @integration_tests/sql/oracle/schema_full.sql

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - uses: actions/setup-go@v7
        with:
          go-version-file: integration_tests/go-godror-oracle/go.mod
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"
      # csproj.jinja pins <TargetFramework>net10.0</TargetFramework>. Without this
      # check a dotnet-version drift back toward 8.0 above could still build
      # silently by falling back to a pre-existing SDK 10 on the runner image,
      # hiding the drift instead of failing it.
      - name: Verify the installed .NET SDK matches the declared TargetFramework
        run: |
          set -euo pipefail
          if ! dotnet --list-sdks | grep -qE '^10\.'; then
            echo "::error::No .NET 10 SDK resolved; csproj.jinja's net10.0 TargetFramework requires one."
            exit 1
          fi
      - uses: erlef/setup-beam@v1
        with:
          otp-version: "26"
          elixir-version: "1.16"
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          # The pinned Ruby ships a Bundler older than 4.x, but the committed Gemfile.lock files
          # are written by Bundler 4. An older Bundler cannot read a newer
          # lockfile: it re-execs to install the newer one mid-step and then
          # fails to resolve gems. A newer Bundler reads older lockfiles fine.
          bundler: latest

      - run: pip install uv
      # Same pnpm major as ci.yml's generated-query-code gate, so a new pnpm
      # release cannot change the answer under one and not the other. No
      # `runtime` input: `actions/setup-node` above already provides node 22.
      # `install: false` because there is no root package.json -- each project
      # below runs its own `pnpm install`.
      - uses: pnpm/setup@v2
        with:
          version: "11"
          install: false
          cache: true
          cache-dependency-path: integration_tests/*/pnpm-lock.yaml
      # allowBuilds (pnpm-workspace.yaml.jinja:1) first appeared in pnpm 10.26 and
      # became the sole build-permission mechanism in pnpm 11 (onlyBuiltDependencies
      # etc. removed). Below that floor an unknown `allowBuilds` key is silently
      # ignored -- exit 0, no warning -- and better-sqlite3/oracledb's postinstall
      # builds are skipped.
      - name: Verify pnpm resolved a version that honours allowBuilds
        run: |
          set -euo pipefail
          resolved="$(pnpm --version)"
          major="${resolved%%.*}"
          rest="${resolved#*.}"
          minor="${rest%%.*}"
          if [ "$major" -lt 10 ] || { [ "$major" -eq 10 ] && [ "$minor" -lt 26 ]; }; then
            echo "::error::pnpm $resolved predates allowBuilds (needs >=10.26); pnpm-workspace.yaml's allowBuilds key would be silently ignored."
            exit 1
          fi

      - name: Set Oracle library paths
        run: |
          echo "LD_LIBRARY_PATH=/usr/lib/oracle/23/client64/lib:${LD_LIBRARY_PATH:-}" >> "$GITHUB_ENV"
          echo "RUSTFLAGS=-L/usr/lib/oracle/23/client64/lib" >> "$GITHUB_ENV"
          echo "/usr/lib/oracle/23/client64/bin" >> "$GITHUB_PATH"

      - name: Test python-oracledb-oracle
        working-directory: integration_tests/python-oracledb-oracle
        run: uv run python test_integration.py

      - name: Test typescript-oracledb-oracle
        working-directory: integration_tests/typescript-oracledb-oracle
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test go-godror-oracle
        working-directory: integration_tests/go-godror-oracle
        run: go mod tidy && go run .

      - name: Test java-jdbc-oracle
        working-directory: integration_tests/java-jdbc-oracle
        run: mvn -q compile exec:java

      - name: Test kotlin-jdbc-oracle
        working-directory: integration_tests/kotlin-jdbc-oracle
        run: gradle run --quiet

      - name: Test csharp-oracle
        working-directory: integration_tests/csharp-oracle
        run: dotnet run

      # jamdb_oracle is a pure Elixir/Erlang client speaking Oracle's wire
      # protocol directly (no OCI/Instant Client dependency), so it needs
      # nothing beyond the setup-beam step already in this job.
      - name: Test elixir-jamdb-oracle
        working-directory: integration_tests/elixir-jamdb-oracle
        run: |
          mix deps.get --quiet
          mix run test/integration_test.exs

      - name: Test rust-sibyl-oracle
        working-directory: integration_tests/rust-sibyl-oracle
        run: cargo run --quiet

      # Deliberately last in this job. ruby-oci8 needs the Oracle Instant Client,
      # which Oracle ships for no macOS ARM64 -- so this leg cannot be run before
      # pushing, and CI is its only verification. Ordering it after every other
      # Oracle step means a failure here costs only its own coverage rather than
      # silently skipping the steps behind it, which is what happened when it sat
      # ahead of rust-sibyl-oracle (board #225).
      - name: Test ruby-oci8-oracle
        working-directory: integration_tests/ruby-oci8-oracle
        run: |
          bundle install
          bundle exec ruby test_integration.rb

  integration-mssql:
    name: MSSQL Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      mssql:
        image: mcr.microsoft.com/mssql/server:2022-latest
        env:
          ACCEPT_EULA: "Y"
          MSSQL_SA_PASSWORD: "Scythe_Test1"
        ports:
          - 1433:1433
        options: >-
          --health-cmd "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'Scythe_Test1' -C -Q 'SELECT 1' -b"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    env:
      MSSQL_URL: "sqlserver://sa:Scythe_Test1@localhost:1433?database=scythe_test"

    steps:
      - uses: actions/checkout@v7.0.1

      - name: Install sqlcmd
        run: |
          curl https://packages.microsoft.com/keys/microsoft.asc | sudo tee /etc/apt/trusted.gpg.d/microsoft.asc
          curl "https://packages.microsoft.com/config/ubuntu/$(lsb_release -rs)/prod.list" | sudo tee /etc/apt/sources.list.d/mssql-release.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y mssql-tools18

      - name: Create database and run schema migration
        run: |
          /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "Scythe_Test1" -C -Q "CREATE DATABASE scythe_test"
          /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "Scythe_Test1" -C -d scythe_test -i integration_tests/sql/mssql/schema.sql

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            integration_tests/rust-tiberius-mssql -> target
      - uses: actions/setup-go@v7
        with:
          go-version-file: integration_tests/go-database-sql-mssql/go.mod
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"
      # csproj.jinja pins <TargetFramework>net10.0</TargetFramework>. Without this
      # check a dotnet-version drift back toward 8.0 above could still build
      # silently by falling back to a pre-existing SDK 10 on the runner image,
      # hiding the drift instead of failing it.
      - name: Verify the installed .NET SDK matches the declared TargetFramework
        run: |
          set -euo pipefail
          if ! dotnet --list-sdks | grep -qE '^10\.'; then
            echo "::error::No .NET 10 SDK resolved; csproj.jinja's net10.0 TargetFramework requires one."
            exit 1
          fi
      - uses: erlef/setup-beam@v1
        with:
          otp-version: "26"
          elixir-version: "1.16"
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          # The pinned Ruby ships a Bundler older than 4.x, but the committed Gemfile.lock files
          # are written by Bundler 4. An older Bundler cannot read a newer
          # lockfile: it re-execs to install the newer one mid-step and then
          # fails to resolve gems. A newer Bundler reads older lockfiles fine.
          bundler: latest
      - uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: pdo_sqlsrv

      - run: pip install uv
      # Same pnpm major as ci.yml's generated-query-code gate, so a new pnpm
      # release cannot change the answer under one and not the other. No
      # `runtime` input: `actions/setup-node` above already provides node 22.
      # `install: false` because there is no root package.json -- each project
      # below runs its own `pnpm install`.
      - uses: pnpm/setup@v2
        with:
          version: "11"
          install: false
          cache: true
          cache-dependency-path: integration_tests/*/pnpm-lock.yaml
      # allowBuilds (pnpm-workspace.yaml.jinja:1) first appeared in pnpm 10.26 and
      # became the sole build-permission mechanism in pnpm 11 (onlyBuiltDependencies
      # etc. removed). Below that floor an unknown `allowBuilds` key is silently
      # ignored -- exit 0, no warning -- and better-sqlite3/oracledb's postinstall
      # builds are skipped.
      - name: Verify pnpm resolved a version that honours allowBuilds
        run: |
          set -euo pipefail
          resolved="$(pnpm --version)"
          major="${resolved%%.*}"
          rest="${resolved#*.}"
          minor="${rest%%.*}"
          if [ "$major" -lt 10 ] || { [ "$major" -eq 10 ] && [ "$minor" -lt 26 ]; }; then
            echo "::error::pnpm $resolved predates allowBuilds (needs >=10.26); pnpm-workspace.yaml's allowBuilds key would be silently ignored."
            exit 1
          fi

      - name: Install ODBC driver for SQL Server
        run: |
          curl https://packages.microsoft.com/keys/microsoft.asc | sudo tee /etc/apt/trusted.gpg.d/microsoft.asc
          curl "https://packages.microsoft.com/config/ubuntu/$(lsb_release -rs)/prod.list" | sudo tee /etc/apt/sources.list.d/mssql-release.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y msodbcsql18 unixodbc-dev

      - name: Test rust-tiberius-mssql
        working-directory: integration_tests/rust-tiberius-mssql
        run: cargo run --quiet

      - name: Test python-pyodbc-mssql
        working-directory: integration_tests/python-pyodbc-mssql
        run: uv run python test_integration.py

      - name: Test typescript-mssql
        working-directory: integration_tests/typescript-mssql
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-kysely-mssql
        working-directory: integration_tests/typescript-kysely-mssql
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test go-database-sql-mssql
        working-directory: integration_tests/go-database-sql-mssql
        run: go mod tidy && go run .

      - name: Test java-jdbc-mssql
        working-directory: integration_tests/java-jdbc-mssql
        run: mvn -q compile exec:java

      - name: Test kotlin-jdbc-mssql
        working-directory: integration_tests/kotlin-jdbc-mssql
        run: gradle run --quiet

      - name: Test csharp-sqlclient-mssql
        working-directory: integration_tests/csharp-sqlclient-mssql
        run: dotnet run

      - name: Test elixir-tds-mssql
        working-directory: integration_tests/elixir-tds-mssql
        run: |
          mix deps.get --quiet
          mix run test/integration_test.exs

      - name: Test ruby-tiny-tds-mssql
        working-directory: integration_tests/ruby-tiny-tds-mssql
        run: |
          bundle install
          bundle exec ruby test_integration.rb

      - name: Test php-pdo-mssql
        working-directory: integration_tests/php-pdo-mssql
        run: php test_integration.php

  integration-duckdb:
    name: DuckDB Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    # No service container: DuckDB is embedded. That is why this group was the
    # cheapest of the orphan-manifest sets to close (issue #126).
    #
    # No DATABASE_URL either -- each harness opens its own in-memory database,
    # except the Go one, which uses a file because database/sql pools
    # connections and each pooled connection would get its own empty
    # in-memory database.

    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-go@v7
        with:
          go-version-file: integration_tests/go-database-sql-duckdb/go.mod

      - run: pip install uv
      - uses: pnpm/setup@v2
        with:
          version: "11"
          install: false
          cache: true
          cache-dependency-path: integration_tests/*/pnpm-lock.yaml
      # allowBuilds (pnpm-workspace.yaml.jinja:1) first appeared in pnpm 10.26 and
      # became the sole build-permission mechanism in pnpm 11 (onlyBuiltDependencies
      # etc. removed). Below that floor an unknown `allowBuilds` key is silently
      # ignored -- exit 0, no warning -- and better-sqlite3/oracledb's postinstall
      # builds are skipped.
      - name: Verify pnpm resolved a version that honours allowBuilds
        run: |
          set -euo pipefail
          resolved="$(pnpm --version)"
          major="${resolved%%.*}"
          rest="${resolved#*.}"
          minor="${rest%%.*}"
          if [ "$major" -lt 10 ] || { [ "$major" -eq 10 ] && [ "$minor" -lt 26 ]; }; then
            echo "::error::pnpm $resolved predates allowBuilds (needs >=10.26); pnpm-workspace.yaml's allowBuilds key would be silently ignored."
            exit 1
          fi

      - name: Test python-duckdb
        working-directory: integration_tests/python-duckdb
        run: uv run python test_integration.py

      - name: Test typescript-duckdb
        working-directory: integration_tests/typescript-duckdb
        run: |
          pnpm install --frozen-lockfile
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test java-jdbc-duckdb
        working-directory: integration_tests/java-jdbc-duckdb
        run: mvn -q compile exec:java

      - name: Test kotlin-jdbc-duckdb
        working-directory: integration_tests/kotlin-jdbc-duckdb
        run: gradle run --quiet

      - name: Test go-database-sql-duckdb
        working-directory: integration_tests/go-database-sql-duckdb
        run: go mod tidy && go run .

  integration-sqlite:
    name: SQLite Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      DATABASE_URL: "sqlite:///tmp/scythe_test.db"
      SQLITE_PATH: "/tmp/scythe_test.db"
      DATABASE_PATH: "/tmp/scythe_test.db"

    steps:
      - uses: actions/checkout@v7.0.1

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            integration_tests/rust-sqlx-sqlite -> target
      - uses: actions/setup-go@v7
        with:
          go-version-file: integration_tests/go-database-sql-sqlite/go.mod
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"
      # csproj.jinja pins <TargetFramework>net10.0</TargetFramework>. Without this
      # check a dotnet-version drift back toward 8.0 above could still build
      # silently by falling back to a pre-existing SDK 10 on the runner image,
      # hiding the drift instead of failing it.
      - name: Verify the installed .NET SDK matches the declared TargetFramework
        run: |
          set -euo pipefail
          if ! dotnet --list-sdks | grep -qE '^10\.'; then
            echo "::error::No .NET 10 SDK resolved; csproj.jinja's net10.0 TargetFramework requires one."
            exit 1
          fi
      - uses: erlef/setup-beam@v1
        with:
          otp-version: "26"
          elixir-version: "1.16"
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          # The pinned Ruby ships a Bundler older than 4.x, but the committed Gemfile.lock files
          # are written by Bundler 4. An older Bundler cannot read a newer
          # lockfile: it re-execs to install the newer one mid-step and then
          # fails to resolve gems. A newer Bundler reads older lockfiles fine.
          bundler: latest
      - uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: pdo_sqlite

      - run: pip install uv
      # Same pnpm major as ci.yml's generated-query-code gate, so a new pnpm
      # release cannot change the answer under one and not the other. No
      # `runtime` input: `actions/setup-node` above already provides node 22.
      # `install: false` because there is no root package.json -- each project
      # below runs its own `pnpm install`.
      - uses: pnpm/setup@v2
        with:
          version: "11"
          install: false
          cache: true
          cache-dependency-path: integration_tests/*/pnpm-lock.yaml
      # allowBuilds (pnpm-workspace.yaml.jinja:1) first appeared in pnpm 10.26 and
      # became the sole build-permission mechanism in pnpm 11 (onlyBuiltDependencies
      # etc. removed). Below that floor an unknown `allowBuilds` key is silently
      # ignored -- exit 0, no warning -- and better-sqlite3/oracledb's postinstall
      # builds are skipped.
      - name: Verify pnpm resolved a version that honours allowBuilds
        run: |
          set -euo pipefail
          resolved="$(pnpm --version)"
          major="${resolved%%.*}"
          rest="${resolved#*.}"
          minor="${rest%%.*}"
          if [ "$major" -lt 10 ] || { [ "$major" -eq 10 ] && [ "$minor" -lt 26 ]; }; then
            echo "::error::pnpm $resolved predates allowBuilds (needs >=10.26); pnpm-workspace.yaml's allowBuilds key would be silently ignored."
            exit 1
          fi

      - name: Create SQLite database
        run: touch /tmp/scythe_test.db

      - name: Test rust-sqlx-sqlite
        working-directory: integration_tests/rust-sqlx-sqlite
        run: cargo run --quiet

      - name: Test go-database-sql-sqlite
        working-directory: integration_tests/go-database-sql-sqlite
        run: go mod tidy && go run .

      - name: Test python-aiosqlite
        working-directory: integration_tests/python-aiosqlite
        run: uv run python test_integration.py

      - name: Test typescript-better-sqlite3
        working-directory: integration_tests/typescript-better-sqlite3
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-kysely-sqlite
        working-directory: integration_tests/typescript-kysely-sqlite
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-node-sqlite
        working-directory: integration_tests/typescript-node-sqlite
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-wasm-sqlite
        working-directory: integration_tests/typescript-wasm-sqlite
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test java-jdbc-sqlite
        working-directory: integration_tests/java-jdbc-sqlite
        run: mvn -q compile exec:java

      - name: Test kotlin-jdbc-sqlite
        working-directory: integration_tests/kotlin-jdbc-sqlite
        run: gradle run --quiet

      - name: Test csharp-microsoft-sqlite
        working-directory: integration_tests/csharp-microsoft-sqlite
        run: dotnet run

      - name: Test elixir-exqlite
        working-directory: integration_tests/elixir-exqlite
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get
          mix run test/integration_test.exs

      - name: Test ruby-sqlite3
        working-directory: integration_tests/ruby-sqlite3
        run: |
          bundle install
          bundle exec ruby test_integration.rb

      - name: Test php-pdo-sqlite
        working-directory: integration_tests/php-pdo-sqlite
        run: php test_integration.php

  integration-redshift:
    name: Redshift Integration Tests (PG-compat)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: scythe
          POSTGRES_PASSWORD: scythe
          POSTGRES_DB: scythe_redshift_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      REDSHIFT_URL: "postgres://scythe:scythe@localhost:5432/scythe_redshift_test"

    steps:
      - uses: actions/checkout@v7.0.1

      - name: Run Redshift PG-compat schema migration
        run: PGPASSWORD=scythe psql -h localhost -U scythe -d scythe_redshift_test -f integration_tests/sql/redshift/schema_pg_compat.sql

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            integration_tests/rust-sqlx-redshift -> target
            integration_tests/rust-tokio-postgres-redshift -> target
      - uses: actions/setup-go@v7
        with:
          go-version-file: integration_tests/go-pgx-redshift/go.mod
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"
      # csproj.jinja pins <TargetFramework>net10.0</TargetFramework>. Without this
      # check a dotnet-version drift back toward 8.0 above could still build
      # silently by falling back to a pre-existing SDK 10 on the runner image,
      # hiding the drift instead of failing it.
      - name: Verify the installed .NET SDK matches the declared TargetFramework
        run: |
          set -euo pipefail
          if ! dotnet --list-sdks | grep -qE '^10\.'; then
            echo "::error::No .NET 10 SDK resolved; csproj.jinja's net10.0 TargetFramework requires one."
            exit 1
          fi
      - uses: erlef/setup-beam@v1
        with:
          otp-version: "26"
          elixir-version: "1.16"
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          # The pinned Ruby ships a Bundler older than 4.x, but the committed Gemfile.lock files
          # are written by Bundler 4. An older Bundler cannot read a newer
          # lockfile: it re-execs to install the newer one mid-step and then
          # fails to resolve gems. A newer Bundler reads older lockfiles fine.
          bundler: latest
      - uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: pdo_pgsql

      - run: pip install uv
      # Same pnpm major as ci.yml's generated-query-code gate, so a new pnpm
      # release cannot change the answer under one and not the other. No
      # `runtime` input: `actions/setup-node` above already provides node 22.
      # `install: false` because there is no root package.json -- each project
      # below runs its own `pnpm install`.
      - uses: pnpm/setup@v2
        with:
          version: "11"
          install: false
          cache: true
          cache-dependency-path: integration_tests/*/pnpm-lock.yaml
      # allowBuilds (pnpm-workspace.yaml.jinja:1) first appeared in pnpm 10.26 and
      # became the sole build-permission mechanism in pnpm 11 (onlyBuiltDependencies
      # etc. removed). Below that floor an unknown `allowBuilds` key is silently
      # ignored -- exit 0, no warning -- and better-sqlite3/oracledb's postinstall
      # builds are skipped.
      - name: Verify pnpm resolved a version that honours allowBuilds
        run: |
          set -euo pipefail
          resolved="$(pnpm --version)"
          major="${resolved%%.*}"
          rest="${resolved#*.}"
          minor="${rest%%.*}"
          if [ "$major" -lt 10 ] || { [ "$major" -eq 10 ] && [ "$minor" -lt 26 ]; }; then
            echo "::error::pnpm $resolved predates allowBuilds (needs >=10.26); pnpm-workspace.yaml's allowBuilds key would be silently ignored."
            exit 1
          fi

      - name: Test rust-sqlx-redshift
        working-directory: integration_tests/rust-sqlx-redshift
        run: cargo run --quiet

      - name: Test rust-tokio-postgres-redshift
        working-directory: integration_tests/rust-tokio-postgres-redshift
        run: cargo run --quiet

      - name: Test python-psycopg3-redshift
        working-directory: integration_tests/python-psycopg3-redshift
        run: uv run python test_integration.py

      - name: Test python-asyncpg-redshift
        working-directory: integration_tests/python-asyncpg-redshift
        run: uv run python test_integration.py

      - name: Test typescript-pg-redshift
        working-directory: integration_tests/typescript-pg-redshift
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-postgres-redshift
        working-directory: integration_tests/typescript-postgres-redshift
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test typescript-kysely-redshift
        working-directory: integration_tests/typescript-kysely-redshift
        run: |
          pnpm install
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test go-pgx-redshift
        working-directory: integration_tests/go-pgx-redshift
        run: go mod tidy && go run .

      - name: Test java-jdbc-redshift
        working-directory: integration_tests/java-jdbc-redshift
        run: mvn -q compile exec:java

      - name: Test kotlin-jdbc-redshift
        working-directory: integration_tests/kotlin-jdbc-redshift
        run: gradle -q run

      - name: Test csharp-npgsql-redshift
        working-directory: integration_tests/csharp-npgsql-redshift
        run: dotnet run

      - name: Test elixir-postgrex-redshift
        working-directory: integration_tests/elixir-postgrex-redshift
        run: |
          mix deps.get
          mix run test/integration_test.exs

      - name: Test ruby-pg-redshift
        working-directory: integration_tests/ruby-pg-redshift
        run: |
          bundle install
          bundle exec ruby test_integration.rb

      - name: Test php-pdo-redshift
        working-directory: integration_tests/php-pdo-redshift
        run: php test_integration.php

  integration-snowflake:
    name: Snowflake Integration Tests (Emulator)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - uses: actions/setup-go@v7
        with:
          go-version-file: integration_tests/go-gosnowflake/go.mod
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"
      # csproj.jinja pins <TargetFramework>net10.0</TargetFramework>. Without this
      # check a dotnet-version drift back toward 8.0 above could still build
      # silently by falling back to a pre-existing SDK 10 on the runner image,
      # hiding the drift instead of failing it.
      - name: Verify the installed .NET SDK matches the declared TargetFramework
        run: |
          set -euo pipefail
          if ! dotnet --list-sdks | grep -qE '^10\.'; then
            echo "::error::No .NET 10 SDK resolved; csproj.jinja's net10.0 TargetFramework requires one."
            exit 1
          fi

      - run: pip install uv
      # Same pnpm major as ci.yml's generated-query-code gate, so a new pnpm
      # release cannot change the answer under one and not the other. No
      # `runtime` input: `actions/setup-node` above already provides node 22.
      # `install: false` because there is no root package.json -- each project
      # below runs its own `pnpm install`.
      - uses: pnpm/setup@v2
        with:
          version: "11"
          install: false
          cache: true
          cache-dependency-path: integration_tests/*/pnpm-lock.yaml
      # allowBuilds (pnpm-workspace.yaml.jinja:1) first appeared in pnpm 10.26 and
      # became the sole build-permission mechanism in pnpm 11 (onlyBuiltDependencies
      # etc. removed). Below that floor an unknown `allowBuilds` key is silently
      # ignored -- exit 0, no warning -- and better-sqlite3/oracledb's postinstall
      # builds are skipped.
      - name: Verify pnpm resolved a version that honours allowBuilds
        run: |
          set -euo pipefail
          resolved="$(pnpm --version)"
          major="${resolved%%.*}"
          rest="${resolved#*.}"
          minor="${rest%%.*}"
          if [ "$major" -lt 10 ] || { [ "$major" -eq 10 ] && [ "$minor" -lt 26 ]; }; then
            echo "::error::pnpm $resolved predates allowBuilds (needs >=10.26); pnpm-workspace.yaml's allowBuilds key would be silently ignored."
            exit 1
          fi

      - name: Test python-snowflake
        working-directory: integration_tests/python-snowflake
        env:
          SNOWFLAKE_USE_FAKESNOW: "1"
        run: uv run python test_integration.py

      # python-snowflake talks to fakesnow in-process. Every other driver
      # below speaks the real Snowflake REST API over HTTP, so they share one
      # fakesnow server (integration_tests/fakesnow/fakesnow_server.py) that
      # translates fakesnow's native Arrow responses into the plain JSON wire
      # format non-Python Snowflake drivers expect.
      - name: Start shared fakesnow server
        working-directory: integration_tests/fakesnow
        run: |
          uv run fakesnow_server.py &
          echo $! > /tmp/fakesnow.pid
          curl --retry 30 --retry-all-errors --retry-delay 1 --silent --output /dev/null http://127.0.0.1:64616/

      - name: Test typescript-snowflake
        working-directory: integration_tests/typescript-snowflake
        env:
          SNOWFLAKE_URL: snowflake://fake:snow@127.0.0.1:64616/testdb/public?account=fakesnow&protocol=http
        run: |
          pnpm install --frozen-lockfile
          pnpm exec tsc --noEmit
          pnpm exec tsx test.ts

      - name: Test go-gosnowflake
        working-directory: integration_tests/go-gosnowflake
        env:
          SNOWFLAKE_URL: fake:snow@127.0.0.1:64616/testdb/public?account=fakesnow&protocol=http&insecureMode=true
        # `go mod tidy` first, as every other Go step does. The generated go.mod
        # carries direct dependencies only, and Go 1.17+ module-graph pruning
        # needs the indirect set resolved before a build; without this, gosnowflake
        # fails with missing go.sum entries across its Azure/AWS/Arrow tree.
        run: go mod tidy && go run .

      # ~keep Both JDBC suites need fakesnow's native Arrow result format, which it
      # now serves to snowflake-jdbc only (dispatched per session on the login
      # request's CLIENT_APP_ID) — the Node, Go and .NET drivers keep the JSON
      # path. snowflake-jdbc also cannot read a temporal column via
      # getObject(col, LocalDateTime.class), so generated code uses the legacy
      # java.sql getters for this engine.
      - name: Test java-jdbc-snowflake
        working-directory: integration_tests/java-jdbc-snowflake
        env:
          SNOWFLAKE_URL: snowflake://fake:snow@127.0.0.1:64616/testdb/public?account=fakesnow&protocol=http
        run: mvn -q compile exec:java

      - name: Test kotlin-jdbc-snowflake
        working-directory: integration_tests/kotlin-jdbc-snowflake
        env:
          SNOWFLAKE_URL: snowflake://fake:snow@127.0.0.1:64616/testdb/public?account=fakesnow&protocol=http
        run: gradle run --quiet

      # csharp-snowflake was excluded until 0.14.0 on the belief that fakesnow's
      # binding-name heuristic was at fault for reading p1/p2/p3 as named rather
      # than positional bindings. That diagnosis was wrong: Snowflake's REST
      # protocol keys positional `?` bindings by bare ordinal, so fakesnow was
      # right to reject `p1` and the generated code had been wrong since v0.6.0 --
      # against real Snowflake too, not just the emulator. It was never caught
      # because this backend had never run anywhere. Now it does.
      - name: Test csharp-snowflake
        working-directory: integration_tests/csharp-snowflake
        env:
          SNOWFLAKE_URL: snowflake://fake:snow@127.0.0.1:64616/testdb/public?account=fakesnow&protocol=http
        run: dotnet run

      - name: Stop shared fakesnow server
        if: always()
        run: kill "$(cat /tmp/fakesnow.pid)" 2>/dev/null || true
